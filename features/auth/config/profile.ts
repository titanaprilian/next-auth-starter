/**
 * Profile page configuration.
 */

export const profileConfig = {
  pageTitleKey: "auth.profile.pageTitle",
  fields: {
    name: "auth.profile.name",
    email: "auth.profile.email",
    role: "auth.profile.role",
  },
};
